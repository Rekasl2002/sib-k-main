<style>
  :root{ --fg:#111; --muted:#555; --line:#ddd; --brand:#0ea5a8;}
  *{ box-sizing:border-box; }
  body{ font-family: DejaVu Sans, Arial, sans-serif; font-size:12px; color:var(--fg); }
  .header{ display:flex; align-items:center; gap:12px; border-bottom:1px solid var(--line); padding-bottom:8px; margin-bottom:12px;}
  .header .meta{ line-height:1.3;}
  .title{ margin:4px 0 2px; font-size:18px; font-weight:700; }
  .sub{ color:var(--muted); font-size:12px;}
  img.logo{ height:42px; }
  .grid{ display:flex; gap:12px; }
  .card{ border:1px solid var(--line); padding:8px; border-radius:6px; }
  .card h4{ margin:0 0 6px; font-size:13px; }
  table{ width:100%; border-collapse:collapse; margin:8px 0 12px; }
  th,td{ border:1px solid var(--line); padding:6px; vertical-align:top; }
  th{ background:#f7f7f7; text-align:left; }
  .stat{ display:flex; gap:10px; margin:6px 0; }
  .stat .box{ border:1px solid var(--line); border-left:4px solid var(--brand); padding:6px 8px; border-radius:4px; }
  .small{ font-size:11px; color:var(--muted); }
  .right{ text-align:right; }
  .center{ text-align:center; }
</style>
